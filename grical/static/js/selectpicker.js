$(document).ready( function() {
    var select = $( "select" );
    select.addClass('selectpicker');
    $('.selectpicker').selectpicker({
        style: 'btn-outline-primary select-button'
    });
});
